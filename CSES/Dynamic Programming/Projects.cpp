#include <bits/stdc++.h>
using namespace std;
const int MAXN = 2e5 + 1;
int a[MAXN][3];

int main() {
    int n; cin >> n; for (int i = 0; i < n; ++i) cin >> a[i][0] >> a[i][1] >> a[i][2];
    
    return 0;
}